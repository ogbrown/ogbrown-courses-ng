
export class Technology {
  public name: string;
  public version: string;
  public infoUrl: string;

  constructor(name: string, version: string, infoUrl: string) {
    this.name = name;
    this.version = version;
    this.infoUrl = infoUrl;
  }
}
